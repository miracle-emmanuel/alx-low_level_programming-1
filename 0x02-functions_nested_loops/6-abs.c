#include "main.h"

/**
 * _abs - absolute value
 * @c: value
 * Return: abs value or number or zero
 */
int _abs(int)
{
	if (c < 0)
	{
		int abs_val;

		abs_val = c * -1;
		return (abs_val);
	}
	return (c);
}
